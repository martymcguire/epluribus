<% content_for :title do %>Editors list for project <%= ActionView::Base.full_sanitizer.sanitize(@project.id.to_s) %> – <% end %>
<script>
window.ProjectEditorStore = new ProjectEditorStoreDef({
  editors: JSON.parse("<%= escape_javascript(@editors_json.to_json).html_safe %>"),
  form_presenter: JSON.parse("<%= escape_javascript(@form_presenter.to_json).html_safe %>")
});
</script>
<%= react_component('ProjectEditorBox',
      { form_presenter: @form_presenter, editors: @editors_json },
      #{ prerender: true }
    ) %>
