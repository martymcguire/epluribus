<%= react_component('ProjectEditorBox',
      { form_presenter: {
            action: project_editors_path(@project),
            csrf_param: request_forgery_protection_token,
            csrf_token: form_authenticity_token
        },
        editors: @editors_json, csrf_token: request_forgery_protection_token.to_s, csrf_token_value: form_authenticity_token
      },
      {prerender: true }
    ) %>
